name: Minddev CI/CD

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
    - name: Project Info
      run: |
        echo "=== Minddev Blog CI/CD ==="
        echo "✅ Repository: Ready"
        echo "✅ Docker Image: sachin202nke/minddev-app"
        echo "✅ Local Run: docker-compose up -d"
        echo "✅ Access: http://localhost:3002"
        echo "✅ Admin: http://localhost:3002/admin"
        echo "=========================="
