name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Build Info
      run: |
        echo "🚀 Building Minddev Blog"
        echo "📦 Image: sachin202nke/minddev-app"
        echo "✅ CI/CD Pipeline Active"
